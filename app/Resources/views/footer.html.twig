<footer class="jumbotron " >
	<div class="container">
    <a href="{{ path('fos_user_security_login') }}">Admin</a>
	{% if is_granted('ROLE_USER') %}
		<a href="{{ path('fos_user_security_logout') }}">Déconnexion</a>
	{% endif %}
	<div class="clock" style="margin:2em;"></div>
</div>
</footer>
