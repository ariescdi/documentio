{% extends '::base_admin.html.twig' %}
{% block h1 -%}
    MediaCategory creation
{% endblock %}
{% block body -%}
    {{ form(form) }}
    <div class="btn-toolbar">
        <div class="btn-group">
            <div class="btn btn-default">
                <a href="{{ path('mediacategory') }}">
                    <i class="glyphicon glyphicon-backward"></i> Back to the list
                </a>
            </div>
        </div>
    </div>
{% endblock %}
