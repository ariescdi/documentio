{% extends '::base.html.twig' %}

{% block body -%}
    <h1>MediaCategory creation</h1>

    {{ form(form) }}

    <div class="btn-toolbar">
        <div class="btn-group">
            <div class="btn btn-default">
                <a href="{{ path('mediacategory') }}">
                    <i class="glyphicon glyphicon-backward"></i> Back to the list
                </a>
            </div>
        </div>
    </div>
{% endblock %}
