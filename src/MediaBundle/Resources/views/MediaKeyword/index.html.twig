{% extends '::base_admin.html.twig' %}
{% block h1 -%}
    Liste des mots clefs
{% endblock %}
{% block body -%}
    <p>Il y a {{count}} {% if count > 1 %}mots clés{% else %}mot clé{% endif %}.</p>

    <table class="records_list table table-hover" data-sort-name="name" data-sort-order="id">
        <thead>
            <tr>
                <th data-field="word" data-align="right" data-sortable="true">Word</th>
                <th data-field="actions" data-align="center" data-sortable="false">Actions</th>
            </tr>
        </thead>
        <tbody>
            {% for entity in entities %}
                <tr>
                    <td><a href="{{ path('mediakeyword_show', { 'id': entity.id }) }}">{{ entity.word }}</a></td>
                    <td>
                        <div class="btn-group">
                            <a class="btn btn-primary" href="#"><i class="glyphicon glyphicon-edit icon-white"></i> Actions</a>
                            <a class="btn btn-primary dropdown-toggle" data-toggle="dropdown" href="#"><span class="caret"></span></a>
                            <ul class="dropdown-menu">
                                <li>
                                    <a href="{{ path('mediakeyword_show', { 'id': entity.id }) }}">Voir</a>
                                </li>
                                <li>
                                    <a href="{{ path('mediakeyword_edit', { 'id': entity.id }) }}">Editer</a>
                                </li>
                            </ul>
                        </div>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

    <div class="btn btn-default">
        <a href="{{ path('mediakeyword_new') }}">
            <i class="glyphicon glyphicon-plus"></i> Créer un nouveau mot clef
        </a>
    </div>
{% endblock %}
