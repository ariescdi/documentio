{% extends '::base_admin.html.twig' %}
{% block h1 -%}
    <h1>Créer un nouveau mot clef</h1>
{% endblock %}
{% block body -%}
    {{ form(form) }}
    <div class="btn-toolbar">
        <div class="btn-group">
            <div class="btn btn-default">
                <a href="{{ path('mediakeyword') }}">
                    <i class="glyphicon glyphicon-backward"></i> Retour à la liste
                </a>
            </div>
        </div>
    </div>
{% endblock %}
