{% extends '::base_admin.html.twig' %}
{% block h1 -%}
    <h1>Media list</h1>
{% endblock %}
{% block body -%}
    <table class="records_list table table-hover" data-height="299" data-sort-name="name" data-sort-order="id">
        <thead>
            <tr>
                <th data-field="id" data-align="right" data-sortable="true">Id</th>
                <th data-field="name" data-align="right" data-sortable="true">Name</th>
                <th data-field="comment" data-align="right" data-sortable="true">Comment</th>
                <th data-field="path" data-align="right" data-sortable="true">Path</th>
                <th data-field="creation_date" data-align="right" data-sortable="true">Creation_date</th>
                <th data-field="update_date" data-align="right" data-sortable="true">Update_date</th>
                <th data-field="mark" data-align="right" data-sortable="true">Mark</th>
                <th data-field="actions" data-align="center" data-sortable="false">Actions</th>
            </tr>
        </thead>
        <tbody>
            {% for entity in entities %}
                <tr>
                    <td><a href="{{ path('media_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                    <td>{{ entity.name }}</td>
                    <td>{{ entity.comment }}</td>
                    <td>{{ entity.path }}</td>
                    <td>{% if entity.creationdate %}{{ entity.creationdate|date('Y-m-d H:i:s') }}{% endif %}</td>
                    <td>{% if entity.updatedate %}{{ entity.updatedate|date('Y-m-d H:i:s') }}{% endif %}</td>
                    <td>{{ entity.mark }}</td>
                    <td style='min-width: 132px'>
                        <div class="btn-group">
                            <a class="btn btn-primary" href="#"><i class="glyphicon glyphicon-edit icon-white"></i> Actions</a>
                            <a class="btn btn-primary dropdown-toggle" data-toggle="dropdown" href="#"><span class="caret"></span></a>
                            <ul class="dropdown-menu">
                                <li>
                                    <a href="{{ path('media_show', { 'id': entity.id }) }}">show</a>
                                </li>
                                <li>
                                    <a href="{{ path('media_edit', { 'id': entity.id }) }}">edit</a>
                                </li>
                            </ul>
                        </div>
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

    <div class="btn btn-default">
        <a href="{{ path('media_new') }}">
            <i class="glyphicon glyphicon-plus"></i> Create a new entry
        </a>
    </div>
    <ul class="list-group">
        {% for entity in entities %}
            <li class="list-group-item expandable">
                <div class="container" style="cursor: pointer" onclick="window.location.href = '{{ path('media_display', { 'id': entity.id }) }}';">
                    <div class="row" style="height:100%; margin: auto 0;">
                        <div class="col-lg-4" style="height:100%; margin: auto 0;">
                            <div class='row' style="height:100%; margin: auto 0;">
                                <div class='row'>
                                    <button class="btn btn-primary" type="button" style='width: 100%'>
                                        {{ entity.name }} <span class="badge" style="
                                              {% if entity.mark == 0 %}
                                                  color: blue;
                                              {% elseif entity.mark > 0 %}
                                                  color: green;
                                              {% else %}
                                                  color: red;
                                              {% endif %}
                                              ">{{ entity.mark }}</span>
                                    </button>
                                </div>
                                <div class='row'>
                                    {{ entity.comment }}
                                </div>
                            </div>
                            <div class='row' style="height:60px;">
                                <div class="col-xs-4">
                                    <a class="btn btn-default" href="{{ asset(entity.webpath) }}" download>Download</a>
                                </div>
                                <div class="col-xs-8">
                                    <div class='btn-group' >
                                        <a class="btn btn-default" style="
                                           {% if entity.mark == 0 %}
                                               color: blue;
                                           {% elseif entity.mark > 0 %}
                                               color: green;
                                           {% else %}
                                               color: red;
                                           {% endif %}
                                           ">{{ entity.mark }}</a>
                                        <a class="btn btn-default glyphicon glyphicon-hand-down" href="{{ path('media_decrease_mark', { 'id': entity.id }) }}" title="J'aime pas !"></a>
                                        <a class="btn btn-default glyphicon glyphicon-hand-up" href="{{ path('media_increase_mark', { 'id': entity.id }) }}" title="J'aime !"></a>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="well col-lg-8" style="overflow: auto; max-height: 200px">
                            {#{{ render(controller("MediaBundle:Media" ~ entity.type.name ~ ":preview",{"id": entity.id})) }}#}
                            <object data='{{ asset(entity.webpath) }}#view=FitH'
                                    type='{{ entity.type.mimetype }}'
                                    width='100%'
                                    height='100%'>

                                <p>It appears your Web browser is not configured to display {{ entity.type.name }} files.
                                    No worries, just <a href='{{ asset(entity.webpath) }}'>click here to download {{ entity.name }}.</a>
                                    Or here to <a class="plugin-search" mimetype="{{ entity.type.mimetype }}">search for plugin</a>.
                                </p>

                            </object>
                        </div>
                    </div>
                </div>
            </li>
        {% endfor %}
    </ul>
    <div class="well">
        <form action="{{ path('media_drop') }}"
              method="POST"
              class="dropzone"
              style="background-image: none; min-height: 64px;"
              id="media_dropzone">
        </form>
    </div>
{% endblock %}

{% block javascripts -%}

    {{ parent() }}

    <script type="text/javascript">

        Dropzone.autoDiscover = false;

        new Dropzone("#media_dropzone", {
            maxFilesize: 100, // MB
            dictDefaultMessage: "Dropzone",
            init: function () {
                console.log("Initializing Dropzone");
                this.on("success", function (file, responseText) {
                    console.log("Dropzone upload successful");
                    document.write(responseText);
                    document.close();
                    window.scrollTo(0, 0);
                });
            }
        });
    </script>
{% endblock %}
